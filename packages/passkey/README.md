# @exortek/passkey

> WebAuthn / FIDO2 server verification for Node.js 22+ — registration + authentication with an in-house CBOR + COSE stack, memory + Redis challenge stores. Zero non-`@exortek/*` runtime dependencies.

Scaffold — implementation lands in follow-up commits (CBOR / COSE / authData parsers → registration flow → authentication flow → docs + examples → 1.0.0).
