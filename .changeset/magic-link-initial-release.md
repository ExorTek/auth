---
"@exortek/magic-link": major
---

Initial 1.0.0 release — passwordless email-link auth with HMAC-signed
short-lived tokens, single-use enforcement via atomic Lua CAS, opt-in
per-email rate limiting, email hashed into payload, memory + Redis stores.
