---
'@exortek/passkey': patch
---

Docs only. Align the README with the shared package template (add the
tests / install-size / types badges, the Docs link and a Why section) and
replace the box-drawing section dividers in the README examples with plain
comments. No runtime code changed — the published 1.0.0 tarball only needed
its README refreshed.
