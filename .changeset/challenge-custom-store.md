---
"@exortek/challenge": minor
---

Add `customStore(impl)` — wrap your own `IncrStore` implementation with validation (fails at construction time if `incr` is missing) and transparent sync/async wrapping.
