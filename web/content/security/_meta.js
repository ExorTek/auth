export default {
  index: 'Overview',
  csrf: 'csrf — CSRF tokens',
  'rate-limit': 'rate-limit — throttling',
  headers: 'headers — HTTP security headers',
  cors: 'cors — origin allowlist',
  redirect: 'redirect — open-redirect guard',
  helpers: 'helpers — defensive utilities',
  middleware: 'middleware — framework adapters',
  production: 'production — deployment checklist',
  errors: 'errors — SecurityError',
};
